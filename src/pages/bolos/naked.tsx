import React from 'react';
import Layout from '../../components/Layout';
import BolosCategoria from '../../components/BolosCategoria';
import type { NextPage } from 'next';

const BolosNaked: NextPage = () => {
    return (
        <Layout>
            <BolosCategoria />
        </Layout>
    );
};

export default BolosNaked; 